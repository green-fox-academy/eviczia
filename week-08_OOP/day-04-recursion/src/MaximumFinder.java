import java.util.Arrays;

// Maximum finder
//Write a function that finds the largest element of an array using recursion.
public class MaximumFinder {
    public static void main(String[] args) {
        int[] myArray = {2, 32, 43, 66, 45, 2, 88, 101, 12, 8, 10, 43};
  //      int max = findMax(myArray);
  //      System.out.println(max);

    }

/*    private static int findMax(int[] myArray) {
        if (myArray[myArray.length-1] == 0) {
            return findMax();
        } else {
            return Math.max(findMax(Arrays.asList(myArray)), myArray[myArray.length - 1]);
        }
    }*/
}
